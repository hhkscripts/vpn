# Streaming Media Routes (Local Bypass / Direct ISP)
# Add non-Google streaming domains here if you wish to route them directly
# through the local ISP to optimize bandwidth.
#
# NOTE: Do NOT add YouTube or Google services here because Google's Anycast IP
# infrastructure is shared with Google Cloud / Gemini / Antigravity APIs.

