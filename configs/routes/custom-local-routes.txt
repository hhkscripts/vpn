# Custom Local Bypass Routes (User-Defined)
# Add any additional domains or IPv4 CIDR blocks here that you want to route
# directly through your local ISP (bypassing the VPN).
#
# Examples:
# example.local.mm
# 203.0.113.0/24
